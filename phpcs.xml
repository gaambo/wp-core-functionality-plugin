<?xml version="1.0"?>
<ruleset name="Vanilla WP (PSR2)">
  <description>PHP Coding Standards based on PSR2</description>

  <!-- Use PSR-2 as a base -->
  <rule ref="PSR2"/>

  <!-- Scan only PHP files -->
  <arg name="extensions" value="php"/>

  <!-- Ignore Composer dependencies -->
  <exclude-pattern>vendor/</exclude-pattern>



</ruleset>